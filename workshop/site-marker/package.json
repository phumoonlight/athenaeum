{
  "name": "site-marker",
  "private": true,
  "type": "module",
  "description": "Marks the source as ES modules, so a script run under node can import the extension's own code. The extension itself has no build step and no dependencies; Chrome ignores this file."
}
